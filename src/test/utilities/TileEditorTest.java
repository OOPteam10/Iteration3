package utilities;

import model.MapSubsystem.Location;
import model.MapSubsystem.Map;
import model.TileSubsystem.Terrains.Sea;
import model.TileSubsystem.Tiles.SeaTile;
import model.TileSubsystem.Tiles.Tile;
import org.junit.Test;
import org.mockito.Mockito;

/**
 * Created by cduica on 3/27/17.
 */
public class TileEditorTest {

    @Test
    public void shouldReturnTrueIfCommitSuccessful(){
//        Map map = Mockito.mock(Map.class);
//        Mockito.when(map.addTile(Mockito.any(), Mockito.any())).thenReturn(true);
//        TileEditor.getInstance().init(map);
//        assert (TileEditor.getInstance().commit());
    }

}
